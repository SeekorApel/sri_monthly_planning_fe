import { DotNumberTablePipe } from './dot-number-table.pipe';

describe('DotNumberTablePipe', () => {
  it('create an instance', () => {
    const pipe = new DotNumberTablePipe();
    expect(pipe).toBeTruthy();
  });
});
